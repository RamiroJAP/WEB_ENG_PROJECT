rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Not used (Cloudinary handles uploads). Keep Storage locked down.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
