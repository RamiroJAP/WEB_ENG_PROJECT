<<<<<<< HEAD:.github/workflows/ci-cd.yml
name: CI/CD Pipeline

on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
=======
name: Deploy (Firebase Hosting)

on:
  push:
    branches:
      - Main
>>>>>>> parent of 544e31b (CI/CD):.github/workflows/deploy.yml

concurrency:
  group: firebase-hosting-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  deployments: write
  checks: write
  pull-requests: write

jobs:
<<<<<<< HEAD:.github/workflows/ci-cd.yml
  # ============ VALIDATION STAGE ============
  validate:
    name: Validate Code
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci
        continue-on-error: false

      - name: Check for secrets
        run: |
          if grep -r "FIREBASE_KEY\|API_KEY\|SECRET" src/ --include="*.jsx" --include="*.js" | grep -v "process.env" | grep -v "secrets"; then
            echo "❌ ERROR: Hardcoded secrets found in source code!"
            exit 1
          fi
          echo "✓ No hardcoded secrets detected"

      - name: Lint code
        run: npm run lint

  # ============ BUILD STAGE ============
  build:
    name: Build Project
    runs-on: ubuntu-latest
    needs: validate

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build project
        run: npm run build

      - name: Upload build artifact
        uses: actions/upload-artifact@v3
        with:
          name: dist
          path: dist/
          retention-days: 1

  # ============ TEST STAGE ============
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    needs: validate

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: |
          if npm run | grep -q "test"; then
            npm test
          else
            echo "ℹ️ No test script defined in package.json"
            echo "Add tests when you're ready: npm install --save-dev vitest"
          fi

  # ============ SECURITY SCAN ============
  security:
    name: Security Check
    runs-on: ubuntu-latest
    needs: validate

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Run security audit
        run: npm audit --production --audit-level=moderate 2>&1 || echo "⚠ Audit completed"

      - name: Check for vulnerable dependencies
        run: |
          npm audit --json 2>/dev/null | grep -q '"vulnerabilities"' && echo "⚠ Some vulnerabilities found" || echo "✓ No critical vulnerabilities"

  # ============ DEPLOY STAGE (Main branch only) ============
  deploy:
    name: Deploy to Firebase Hosting
    runs-on: ubuntu-latest
    needs: [build, test, security]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
=======
  deploy:
    name: Deploy to Firebase Hosting
    runs-on: ubuntu-latest
>>>>>>> parent of 544e31b (CI/CD):.github/workflows/deploy.yml

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Download build artifact
        uses: actions/download-artifact@v3
        with:
          name: dist
          path: dist/

      - name: Verify secrets are set
        run: |
          if [ -z "${{ secrets.FIREBASE_SERVICE_ACCOUNT_WEB_ENG_PROJECT_D657D }}" ]; then
            echo "❌ ERROR: Firebase service account not configured!"
            exit 1
          fi
          echo "✓ Secrets verified"

      - name: Deploy to Firebase Hosting
        uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_WEB_ENG_PROJECT_D657D }}
          projectId: web-eng-project-d657d
          channelId: live
          entryPoint: .

      - name: Deployment Success
        run: echo "✅ Successfully deployed to Firebase Hosting"
